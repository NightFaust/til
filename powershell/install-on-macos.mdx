# Powershell on MacOs

Yes, you can run Powershell on MacOS. Here is how you can install it.
```bash
```
brew install --cask powershell
```

To verify the installation, you can run the following command:
```bash
pwsh
```

